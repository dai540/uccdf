# Write a Compact Report

Write a Compact Report

## Usage

``` r
report(fit, file, format = c("md", "html"))
```

## Arguments

- fit:

  A `uccdf_fit` object.

- file:

  Output file path.

- format:

  Either `"md"` or `"html"`.

## Value

Invisibly returns the output path.
